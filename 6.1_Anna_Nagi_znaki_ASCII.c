//Anna Nagi
#include <stdio.h>
int main(void){
    int licznik=0;
    while(licznik<=255){
        printf("Znak to %c, a jego numer to %d\n",licznik,licznik);
        licznik++;
    }
    return 0;
}
