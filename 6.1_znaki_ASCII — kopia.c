//Anna Nagi
#include <stdio.h>

int main(void){
    int licznik;
    for (licznik=0; licznik<=255; licznik++){
        printf("Znak to %c, a jego numer to %d\n", licznik, licznik);
    }
    return 0;
}
